<Window x:Class="ConflictStudio.App.MainWindow" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:local="clr-namespace:ConflictStudio.App" Title="Cyberpunk Conflict Studio" Width="1660" Height="980" MinWidth="1180" MinHeight="760" Background="#070B0F" Foreground="#F2F6F7" FontFamily="Segoe UI">
    <Window.Resources>
        <SolidColorBrush x:Key="Panel" Color="#0D141A" />
        <SolidColorBrush x:Key="Border" Color="#28404D" />
        <SolidColorBrush x:Key="Muted" Color="#8EA2AD" />
        <SolidColorBrush x:Key="Accent" Color="#00F0FF" />
        <SolidColorBrush x:Key="WarningAccent" Color="#FCEE0A" />
        <BooleanToVisibilityConverter x:Key="BoolVisibility" />
        <SolidColorBrush x:Key="{x:Static SystemColors.WindowBrushKey}" Color="#090F14" />
        <SolidColorBrush x:Key="{x:Static SystemColors.WindowTextBrushKey}" Color="#F2F6F7" />
        <SolidColorBrush x:Key="{x:Static SystemColors.ControlBrushKey}" Color="#111B22" />
        <SolidColorBrush x:Key="{x:Static SystemColors.ControlTextBrushKey}" Color="#F2F6F7" />
        <SolidColorBrush x:Key="{x:Static SystemColors.HighlightBrushKey}" Color="#075A66" />
        <SolidColorBrush x:Key="{x:Static SystemColors.HighlightTextBrushKey}" Color="#FFFFFF" />
        <Style TargetType="Button">
            <Setter Property="Background" Value="#14232C" />
            <Setter Property="BorderBrush" Value="#355765" />
            <Setter Property="Foreground" Value="#F2F6F7" />
            <Setter Property="Padding" Value="12,7" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Style.Triggers>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Background" Value="#0D141A" />
                    <Setter Property="Foreground" Value="#617681" />
                    <Setter Property="BorderBrush" Value="#223742" />
                </Trigger>
            </Style.Triggers>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="#090F14" />
            <Setter Property="BorderBrush" Value="#35515E" />
            <Setter Property="Foreground" Value="#F2F6F7" />
            <Setter Property="CaretBrush" Value="#00F0FF" />
            <Setter Property="Padding" Value="8,6" />
        </Style>
        <Style TargetType="ComboBoxItem">
            <Setter Property="Background" Value="#0B1218" />
            <Setter Property="Foreground" Value="#F2F6F7" />
            <Setter Property="Padding" Value="8,6" />
        </Style>
        <Style TargetType="ComboBox">
            <Setter Property="Foreground" Value="#F2F6F7" />
            <Setter Property="Padding" Value="8,6" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <ToggleButton Focusable="False" ClickMode="Press" IsChecked="{Binding IsDropDownOpen, RelativeSource={RelativeSource TemplatedParent}, Mode=TwoWay}">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="ToggleButton">
                                        <Border Background="#090F14" BorderBrush="#35515E" BorderThickness="1" Padding="{TemplateBinding Padding}">
                                            <Grid>
                                                <ContentPresenter Margin="0,0,20,0" VerticalAlignment="Center" Content="{Binding SelectionBoxItem, RelativeSource={RelativeSource AncestorType=ComboBox}}" />
                                                <Path HorizontalAlignment="Right" VerticalAlignment="Center" Fill="#00F0FF" Data="M 0 0 L 8 0 L 4 5 Z" />
                                            </Grid>
                                        </Border>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                            </ToggleButton>
                            <Popup IsOpen="{TemplateBinding IsDropDownOpen}" Placement="Bottom" AllowsTransparency="True" Focusable="False">
                                <Border MinWidth="{Binding ActualWidth, RelativeSource={RelativeSource TemplatedParent}}" MaxHeight="320" Background="#0B1218" BorderBrush="#35515E" BorderThickness="1">
                                    <ScrollViewer>
                                        <ItemsPresenter />
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="TabControl">
            <Setter Property="Background" Value="#0D141A" />
            <Setter Property="BorderBrush" Value="#28404D" />
            <Setter Property="Foreground" Value="#F2F6F7" />
        </Style>
        <Style TargetType="TabItem">
            <Setter Property="Foreground" Value="#AFC0BE" />
            <Setter Property="Padding" Value="18,9" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabItem">
                        <Border x:Name="TabBorder" Padding="{TemplateBinding Padding}" Background="#0B1218" BorderBrush="#28404D" BorderThickness="1,1,1,0">
                            <ContentPresenter ContentSource="Header" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="TabBorder" Property="Background" Value="#10242C" />
                                <Setter Property="Foreground" Value="#FFFFFF" />
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="TabBorder" Property="Background" Value="#111D24" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="ListBox">
            <Setter Property="Background" Value="#090F14" />
            <Setter Property="BorderBrush" Value="#28404D" />
            <Setter Property="Foreground" Value="#F2F6F7" />
        </Style>
        <Style TargetType="TreeView">
            <Setter Property="Background" Value="#090F14" />
            <Setter Property="BorderBrush" Value="#28404D" />
            <Setter Property="Foreground" Value="#F2F6F7" />
            <Setter Property="Padding" Value="8" />
        </Style>
        <Style TargetType="TreeViewItem">
            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
            <Setter Property="Padding" Value="2" />
        </Style>
        <ControlTemplate x:Key="InvisibleScrollTrackButton" TargetType="{x:Type RepeatButton}">
            <Border Background="Transparent" />
        </ControlTemplate>
        <ControlTemplate x:Key="SlimVerticalScrollBar" TargetType="{x:Type ScrollBar}">
            <Grid Background="#080D11">
                <Track x:Name="PART_Track" Orientation="Vertical" IsDirectionReversed="True">
                    <Track.DecreaseRepeatButton><RepeatButton Focusable="False" Template="{StaticResource InvisibleScrollTrackButton}" Command="{x:Static ScrollBar.PageUpCommand}" /></Track.DecreaseRepeatButton>
                    <Track.Thumb>
                        <Thumb MinHeight="18"><Thumb.Template><ControlTemplate TargetType="{x:Type Thumb}"><Border Margin="2,1" Background="#A7C4CC" CornerRadius="2" Opacity="0.38" /></ControlTemplate></Thumb.Template></Thumb>
                    </Track.Thumb>
                    <Track.IncreaseRepeatButton><RepeatButton Focusable="False" Template="{StaticResource InvisibleScrollTrackButton}" Command="{x:Static ScrollBar.PageDownCommand}" /></Track.IncreaseRepeatButton>
                </Track>
            </Grid>
        </ControlTemplate>
        <ControlTemplate x:Key="SlimHorizontalScrollBar" TargetType="{x:Type ScrollBar}">
            <Grid Background="#080D11">
                <Track x:Name="PART_Track" Orientation="Horizontal">
                    <Track.DecreaseRepeatButton><RepeatButton Focusable="False" Template="{StaticResource InvisibleScrollTrackButton}" Command="{x:Static ScrollBar.PageLeftCommand}" /></Track.DecreaseRepeatButton>
                    <Track.Thumb>
                        <Thumb MinWidth="18"><Thumb.Template><ControlTemplate TargetType="{x:Type Thumb}"><Border Margin="1,2" Background="#A7C4CC" CornerRadius="2" Opacity="0.38" /></ControlTemplate></Thumb.Template></Thumb>
                    </Track.Thumb>
                    <Track.IncreaseRepeatButton><RepeatButton Focusable="False" Template="{StaticResource InvisibleScrollTrackButton}" Command="{x:Static ScrollBar.PageRightCommand}" /></Track.IncreaseRepeatButton>
                </Track>
            </Grid>
        </ControlTemplate>
        <Style TargetType="{x:Type ScrollBar}">
            <Setter Property="Background" Value="#080D11" />
            <Style.Triggers>
                <Trigger Property="Orientation" Value="Vertical"><Setter Property="Width" Value="9" /><Setter Property="MinWidth" Value="0" /><Setter Property="MaxWidth" Value="9" /><Setter Property="Template" Value="{StaticResource SlimVerticalScrollBar}" /></Trigger>
                <Trigger Property="Orientation" Value="Horizontal"><Setter Property="Height" Value="9" /><Setter Property="MinHeight" Value="0" /><Setter Property="MaxHeight" Value="9" /><Setter Property="Template" Value="{StaticResource SlimHorizontalScrollBar}" /></Trigger>
            </Style.Triggers>
        </Style>
        <Style x:Key="OverviewScrollBar" TargetType="{x:Type ScrollBar}">
            <Setter Property="Width" Value="10" />
            <Setter Property="Orientation" Value="Vertical" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ScrollBar}">
                        <Grid Background="Transparent">
                            <Track x:Name="PART_Track" IsDirectionReversed="True">
                                <Track.DecreaseRepeatButton>
                                    <RepeatButton Focusable="False" Template="{StaticResource InvisibleScrollTrackButton}" Command="{x:Static ScrollBar.PageUpCommand}" />
                                </Track.DecreaseRepeatButton>
                                <Track.Thumb>
                                    <Thumb>
                                        <Thumb.Template>
                                            <ControlTemplate TargetType="{x:Type Thumb}">
                                                <Border x:Name="ThumbBody" Margin="3,0" Background="#E7F7FA" CornerRadius="2" Opacity="0.18" />
                                            </ControlTemplate>
                                        </Thumb.Template>
                                    </Thumb>
                                </Track.Thumb>
                                <Track.IncreaseRepeatButton>
                                    <RepeatButton Focusable="False" Template="{StaticResource InvisibleScrollTrackButton}" Command="{x:Static ScrollBar.PageDownCommand}" />
                                </Track.IncreaseRepeatButton>
                            </Track>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="DataGrid">
            <Setter Property="Background" Value="#090F14" />
            <Setter Property="BorderBrush" Value="#28404D" />
            <Setter Property="Foreground" Value="#F2F6F7" />
            <Setter Property="RowBackground" Value="#090F14" />
            <Setter Property="AlternatingRowBackground" Value="#0C141A" />
            <Setter Property="GridLinesVisibility" Value="All" />
            <Setter Property="HorizontalGridLinesBrush" Value="#213844" />
            <Setter Property="VerticalGridLinesBrush" Value="#344F5C" />
            <Setter Property="AutoGenerateColumns" Value="False" />
            <Setter Property="IsReadOnly" Value="True" />
        </Style>
        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#111C24" />
            <Setter Property="Foreground" Value="#D8E6EA" />
            <Setter Property="BorderBrush" Value="#46616E" />
            <Setter Property="BorderThickness" Value="0,0,1,1" />
            <Setter Property="Padding" Value="10,7" />
            <Setter Property="SeparatorVisibility" Value="Visible" />
            <Setter Property="SeparatorBrush" Value="#6A8996" />
            <Setter Property="ToolTip" Value="Drag the right edge to resize this column" />
        </Style>
        <DataTemplate DataType="{x:Type local:ArchiveRailItem}">
            <Grid>
                <Border x:Name="RelationshipRow" Padding="5,2" Background="Transparent" BorderBrush="Transparent" BorderThickness="3,0,0,0">
                    <TextBlock Text="{Binding ArchiveName}" TextTrimming="CharacterEllipsis" />
                </Border>
                <Border x:Name="DropBeforeLine" Height="3" VerticalAlignment="Top" Background="#FCEE0A" Visibility="Collapsed" IsHitTestVisible="False" />
                <Border x:Name="DropAfterLine" Height="3" VerticalAlignment="Bottom" Background="#FCEE0A" Visibility="Collapsed" IsHitTestVisible="False" />
            </Grid>
            <DataTemplate.Triggers>
                <DataTrigger Binding="{Binding RelationshipTone}" Value="Wins">
                    <Setter TargetName="RelationshipRow" Property="Background" Value="#08251B" />
                    <Setter TargetName="RelationshipRow" Property="BorderBrush" Value="#42E58B" />
                </DataTrigger>
                <DataTrigger Binding="{Binding RelationshipTone}" Value="Loses">
                    <Setter TargetName="RelationshipRow" Property="Background" Value="#310913" />
                    <Setter TargetName="RelationshipRow" Property="BorderBrush" Value="#FF304F" />
                </DataTrigger>
                <DataTrigger Binding="{Binding RelationshipTone}" Value="Mixed">
                    <Setter TargetName="RelationshipRow" Property="Background" Value="#282500" />
                    <Setter TargetName="RelationshipRow" Property="BorderBrush" Value="#FCEE0A" />
                </DataTrigger>
                <DataTrigger Binding="{Binding RelationshipTone}" Value="Same">
                    <Setter TargetName="RelationshipRow" Property="Background" Value="#062431" />
                    <Setter TargetName="RelationshipRow" Property="BorderBrush" Value="#00C8FF" />
                </DataTrigger>
                <DataTrigger Binding="{Binding RelationshipTone}" Value="Unknown">
                    <Setter TargetName="RelationshipRow" Property="Background" Value="#1D1230" />
                    <Setter TargetName="RelationshipRow" Property="BorderBrush" Value="#B48CFF" />
                </DataTrigger>
                <DataTrigger Binding="{Binding DropCue}" Value="Before"><Setter TargetName="DropBeforeLine" Property="Visibility" Value="Visible" /></DataTrigger>
                <DataTrigger Binding="{Binding DropCue}" Value="After"><Setter TargetName="DropAfterLine" Property="Visibility" Value="Visible" /></DataTrigger>
            </DataTemplate.Triggers>
        </DataTemplate>
        <HierarchicalDataTemplate DataType="{x:Type local:ArchiveConflictNode}" ItemsSource="{Binding Children}">
            <Border x:Name="RelationshipRow" Margin="0,1" Padding="3,2" Background="Transparent" BorderBrush="Transparent" BorderThickness="3,0,0,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="360" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <TextBlock Grid.RowSpan="2" Foreground="#7F918F" Text="{Binding OrderPosition}" Width="38" />
                <TextBlock Grid.Column="1" FontWeight="SemiBold" Text="{Binding ArchiveName}" TextTrimming="CharacterEllipsis" ToolTip="{Binding ArchiveName}" />
                <StackPanel Grid.Column="2" Margin="12,0,4,0" VerticalAlignment="Center" Orientation="Horizontal">
                    <TextBlock Margin="0,0,10,0" FontSize="11" Foreground="#82928F" Text="{Binding CountSummary}" />
                    <Ellipse Width="9" Height="9" Margin="3,0" Fill="#42E58B" Visibility="{Binding HasWinning, Converter={StaticResource BoolVisibility}}" />
                    <Ellipse Width="9" Height="9" Margin="3,0" Fill="#FF304F" Visibility="{Binding HasLosing, Converter={StaticResource BoolVisibility}}" />
                    <Ellipse Width="9" Height="9" Margin="3,0" Fill="#00C8FF" Visibility="{Binding HasSame, Converter={StaticResource BoolVisibility}}" />
                    <Ellipse Width="9" Height="9" Margin="3,0" Fill="#B48CFF" Visibility="{Binding HasUnknown, Converter={StaticResource BoolVisibility}}" />
                </StackPanel>
                <TextBlock Grid.Row="1" Grid.Column="1" Grid.ColumnSpan="2" Margin="0,2,0,0" FontSize="11" Foreground="#82928F" Text="{Binding Provider}" TextTrimming="CharacterEllipsis" ToolTip="{Binding Provider}" />
            </Grid>
            </Border>
            <HierarchicalDataTemplate.Triggers>
                <DataTrigger Binding="{Binding RelationshipTone}" Value="Wins">
                    <Setter TargetName="RelationshipRow" Property="Background" Value="#08251B" />
                    <Setter TargetName="RelationshipRow" Property="BorderBrush" Value="#42E58B" />
                </DataTrigger>
                <DataTrigger Binding="{Binding RelationshipTone}" Value="Loses">
                    <Setter TargetName="RelationshipRow" Property="Background" Value="#310913" />
                    <Setter TargetName="RelationshipRow" Property="BorderBrush" Value="#FF304F" />
                </DataTrigger>
                <DataTrigger Binding="{Binding RelationshipTone}" Value="Mixed">
                    <Setter TargetName="RelationshipRow" Property="Background" Value="#282500" />
                    <Setter TargetName="RelationshipRow" Property="BorderBrush" Value="#FCEE0A" />
                </DataTrigger>
                <DataTrigger Binding="{Binding RelationshipTone}" Value="Same">
                    <Setter TargetName="RelationshipRow" Property="Background" Value="#062431" />
                    <Setter TargetName="RelationshipRow" Property="BorderBrush" Value="#00C8FF" />
                </DataTrigger>
                <DataTrigger Binding="{Binding RelationshipTone}" Value="Unknown">
                    <Setter TargetName="RelationshipRow" Property="Background" Value="#1D1230" />
                    <Setter TargetName="RelationshipRow" Property="BorderBrush" Value="#B48CFF" />
                </DataTrigger>
            </HierarchicalDataTemplate.Triggers>
        </HierarchicalDataTemplate>
        <HierarchicalDataTemplate DataType="{x:Type local:ArchiveConflictGroupNode}" ItemsSource="{Binding Children}">
            <TextBlock Margin="3,3" FontWeight="SemiBold" Text="{Binding Header}">
                <TextBlock.Style>
                    <Style TargetType="TextBlock">
                        <Setter Property="Foreground" Value="#AFC0BE" />
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding Tone}" Value="Winning">
                                <Setter Property="Foreground" Value="#42E58B" />
                            </DataTrigger>
                            <DataTrigger Binding="{Binding Tone}" Value="Losing">
                                <Setter Property="Foreground" Value="#FF4964" />
                            </DataTrigger>
                            <DataTrigger Binding="{Binding Tone}" Value="Same">
                                <Setter Property="Foreground" Value="#00C8FF" />
                            </DataTrigger>
                            <DataTrigger Binding="{Binding Tone}" Value="Unknown">
                                <Setter Property="Foreground" Value="#B48CFF" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </TextBlock.Style>
            </TextBlock>
        </HierarchicalDataTemplate>
        <DataTemplate DataType="{x:Type local:ArchiveResourceNode}">
            <Grid Margin="3,2">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBlock Text="{Binding Path}" TextTrimming="CharacterEllipsis">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Setter Property="Foreground" Value="#AFC0BE" />
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding Tone}" Value="Winning">
                                    <Setter Property="Foreground" Value="#42E58B" />
                                </DataTrigger>
                                <DataTrigger Binding="{Binding Tone}" Value="Losing">
                                    <Setter Property="Foreground" Value="#FF4964" />
                                </DataTrigger>
                                <DataTrigger Binding="{Binding Tone}" Value="Same">
                                    <Setter Property="Foreground" Value="#00C8FF" />
                                </DataTrigger>
                                <DataTrigger Binding="{Binding Tone}" Value="Unknown">
                                    <Setter Property="Foreground" Value="#B48CFF" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>
                <TextBlock Grid.Column="1" Margin="16,0,4,0" Foreground="#7F918F" Text="{Binding ProviderContext}" />
            </Grid>
        </DataTemplate>
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="58" />
            <RowDefinition Height="74" />
            <RowDefinition Height="*" />
            <RowDefinition Height="30" />
        </Grid.RowDefinitions>
        <Border Background="#0B1218" BorderBrush="#28404D" BorderThickness="0,0,0,1">
            <Grid Margin="18,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Foreground="{StaticResource WarningAccent}" FontSize="20" FontWeight="Bold" Text="CONFLICT STUDIO" />
                    <Border Width="1" Height="24" Margin="15,0" Background="#46616E" />
                    <StackPanel>
                        <TextBlock Foreground="{StaticResource Accent}" FontSize="11" FontWeight="SemiBold" Text="CYBERPUNK 2077" />
                        <TextBlock Foreground="{StaticResource Muted}" FontSize="11" Text="MOD CONFLICT CHECKER" />
                    </StackPanel>
                </StackPanel>
            </Grid>
        </Border>
        <Border Grid.Row="1" Padding="18,10" Background="#0B1218" BorderBrush="#28404D" BorderThickness="0,0,0,1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="125" />
                    <ColumnDefinition Width="260" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="260" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <StackPanel>
                    <TextBlock FontSize="10" Foreground="{StaticResource Accent}" Text="MANAGER" />
                    <ComboBox x:Name="ManagerModeComboBox" Margin="0,4,8,0" SelectionChanged="ManagerModeChanged" AutomationProperties.Name="Mod manager">
                        <ComboBoxItem Tag="Mo2" IsSelected="True">MO2</ComboBoxItem>
                        <ComboBoxItem Tag="Vortex">Vortex</ComboBoxItem>
                        <ComboBoxItem Tag="Manual">Manual</ComboBoxItem>
                    </ComboBox>
                </StackPanel>
                <StackPanel Grid.Column="1">
                    <TextBlock x:Name="ManagerLocationLabel" FontSize="10" Foreground="{StaticResource Accent}" Text="MO2 INSTALLATION" />
                    <TextBox x:Name="Mo2RootTextBox" Margin="0,4,0,0" TextChanged="Mo2RootChanged" AutomationProperties.Name="Manager installation or context path" />
                </StackPanel>
                <Button x:Name="FindProfilesButton" Grid.Column="2" Margin="8,16,14,0" Click="DiscoverProfilesClicked" Content="Browse" />
                <StackPanel Grid.Column="3">
                    <TextBlock FontSize="10" Foreground="{StaticResource Accent}" Text="ACTIVE PROFILE" />
                    <ComboBox x:Name="ProfileComboBox" Margin="0,4,0,0" SelectionChanged="ProfileSelectionChanged" AutomationProperties.Name="Active profile" />
                </StackPanel>
                <Button x:Name="ScanProfileButton" Grid.Column="4" Margin="12,16,0,0" Background="#2A2900" BorderBrush="#FCEE0A" Foreground="#FFF67A" Click="UnifiedScanClicked" Content="Scan profile" />
                <Button x:Name="CancelScanButton" Grid.Column="5" Margin="8,16,0,0" IsEnabled="False" Click="CancelScanClicked" Content="Cancel" />
                <StackPanel Grid.Column="6" Margin="18,3,0,0">
                    <TextBlock x:Name="WorkspaceStatusTextBlock" Foreground="#B7C6C4" Text="Choose an installation and profile." TextTrimming="CharacterEllipsis" />
                    <ProgressBar x:Name="ScanProgressBar" Margin="0,9,0,0" Height="4" Minimum="0" Maximum="1" Value="0" Foreground="{StaticResource Accent}" Background="#253235" />
                    <TextBlock x:Name="ScanPhaseTextBlock" Margin="0,5,0,0" FontSize="11" Foreground="{StaticResource Muted}" Text="Ready" />
                </StackPanel>
            </Grid>
        </Border>
        <TabControl x:Name="MainTabControl" Grid.Row="2" Margin="12" Background="{StaticResource Panel}" BorderBrush="#2A393C">
            <TabItem Header="Archives" IsSelected="True">
                <Grid x:Name="ArchiveOrderTabContent">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="390" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <Border Padding="12" Background="#101719" BorderBrush="#2A393C" BorderThickness="0,0,1,0">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="*" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>
                            <StackPanel>
                                <TextBlock FontSize="20" FontWeight="SemiBold" Text="Load order" />
                                <TextBlock Margin="0,4,0,0" Foreground="{StaticResource Muted}" Text="Higher archives win. Ctrl/Shift-select and drag legacy archives; push farther into an edge to scroll faster, or use the mouse wheel while dragging. REDmods keep their separate order." TextWrapping="Wrap" />
                                <Expander Margin="0,5,0,0" Header="How load order works">
                                    <TextBlock Margin="0,5,0,0" Foreground="{StaticResource Muted}" Text="Conflict Studio uses the active archive modlist.txt when it lists every enabled legacy archive exactly once. The first listed archive wins shared files. Without that file, legacy archives use binary ASCII filename order. If the list is incomplete or duplicated, Conflict Studio prepares a complete repair draft for review. Legacy archives load before REDmods, whose order is shown but changed through REDmod deployment." TextWrapping="Wrap" />
                                </Expander>
                            </StackPanel>
                            <TextBox x:Name="ArchiveSearchTextBox" Grid.Row="1" Margin="0,10,0,8" TextChanged="ArchiveSearchTextChanged" ToolTip="Find an archive in load order" AutomationProperties.AutomationId="ArchiveSearch" />
                            <Grid Grid.Row="2">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="12" />
                                </Grid.ColumnDefinitions>
                                <ListBox x:Name="ArchiveOrderListBox" HorizontalContentAlignment="Stretch" ScrollViewer.VerticalScrollBarVisibility="Hidden" SelectionMode="Extended" SelectionChanged="ArchiveOrderSelectionChanged" />
                                <Grid Grid.Column="1">
                                    <local:ArchiveOverviewBar x:Name="LoadOrderOverviewBar" Focusable="False" IsHitTestVisible="False" AutomationProperties.Name="Selected archive relationships in load order" />
                                    <ScrollBar x:Name="LoadOrderScrollBar" Style="{StaticResource OverviewScrollBar}" AutomationProperties.Name="Load-order scrollbar" />
                                </Grid>
                            </Grid>
                            <StackPanel Grid.Row="3" Margin="0,9,0,0">
                                <Border Margin="0,0,0,7" Padding="9" Background="#292600" BorderBrush="#8C8500" BorderThickness="1" Visibility="{Binding HasUnknownImpact, Converter={StaticResource BoolVisibility}}">
                                    <StackPanel>
                                        <TextBlock Foreground="#FFD166" Text="{Binding PreviewStatus}" TextWrapping="Wrap" />
                                        <CheckBox Margin="0,7,0,0" IsChecked="{Binding UnknownImpactAcknowledged, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Content="I understand that unreadable archive impact cannot be previewed" />
                                    </StackPanel>
                                </Border>
                                <Border Padding="9" Background="#15201F" BorderBrush="#31574D" BorderThickness="1" Visibility="{Binding CanApply, Converter={StaticResource BoolVisibility}}">
                                    <StackPanel>
                                        <TextBlock Foreground="#B7C6C4" Text="{Binding PreviewStatus}" TextWrapping="Wrap" />
                                        <TextBlock Margin="0,5,0,0" Foreground="#FFD166" Text="Apply writes this order without renaming archives, creates a backup, verifies the result, and scans again. Cyberpunk must be closed." TextWrapping="Wrap" />
                                        <Expander Margin="0,5,0,0" Header="Changed winners">
                                            <DataGrid Margin="0,5,0,0" MaxHeight="120" ItemsSource="{Binding WinnerDeltas}">
                                                <DataGrid.Columns>
                                                    <DataGridTextColumn Header="File" Binding="{Binding DisplayName}" Width="*" />
                                                    <DataGridTextColumn Header="Before" Binding="{Binding BeforeWinner}" Width="105" />
                                                    <DataGridTextColumn Header="After" Binding="{Binding AfterWinner}" Width="105" />
                                                </DataGrid.Columns>
                                            </DataGrid>
                                        </Expander>
                                        <Button x:Name="ApplyArchiveOrderButton" Margin="0,7,0,0" Background="#1D4B46" BorderBrush="#3D8C7E" Click="ApplyClicked" Content="Apply, verify, and back up" />
                                    </StackPanel>
                                </Border>
                                <Button Margin="0,7,0,0" Visibility="{Binding CanReset, Converter={StaticResource BoolVisibility}}" Click="ResetArchiveOrderClicked" Content="Reset proposed order" />
                                <Button Margin="0,7,0,0" Visibility="{Binding CanUndo, Converter={StaticResource BoolVisibility}}" Click="UndoArchiveOrderClicked" Content="Undo last applied change" />
                            </StackPanel>
                        </Grid>
                    </Border>
                    <Grid Grid.Column="1" Margin="14,10,14,12">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <StackPanel>
                                <TextBlock FontSize="22" FontWeight="SemiBold" Text="Archive conflicts" />
                                <TextBlock x:Name="ArchiveConflictCountTextBlock" Margin="0,3,0,0" Foreground="{StaticResource Muted}" Text="Run a scan to inspect conflicts." />
                                <TextBlock Margin="0,3,0,0" FontSize="11"><Run Foreground="#8EA2AD" Text="Select an archive:  " /><Run Foreground="#42E58B" Text="● it wins" /><Run Foreground="#8EA2AD" Text="   " /><Run Foreground="#FF4964" Text="● it loses" /><Run Foreground="#8EA2AD" Text="   " /><Run Foreground="#00C8FF" Text="● identical" /><Run Foreground="#8EA2AD" Text="   " /><Run Foreground="#B48CFF" Text="● can't determine" /></TextBlock>
                            </StackPanel>
                            <StackPanel Grid.Column="1" Orientation="Horizontal">
                                <Button x:Name="OpenSelectedArchiveButton" Margin="8,0,0,0" Click="OpenSelectedArchiveClicked" Content="Open selected archive" />
                            </StackPanel>
                        </Grid>
                        <Border x:Name="ArchiveOrderEvidenceBorder" Grid.Row="1" Margin="0,9,0,9" Padding="10,7" Background="#13211D" BorderBrush="#31574D" BorderThickness="1">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <StackPanel>
                                    <TextBlock x:Name="ArchiveOrderEvidenceTitleTextBlock" FontWeight="SemiBold" Foreground="#8EDDBF" Text="Archive order not loaded" />
                                    <TextBlock x:Name="ArchiveOrderEvidenceTextBlock" Margin="0,3,0,0" Foreground="#B7C6C4" Text="Run a profile scan." TextWrapping="Wrap" />
                                </StackPanel>
                                <Button x:Name="ArchiveOrderActionButton" Grid.Column="1" Margin="14,0,0,0" VerticalAlignment="Center" Click="FocusArchiveLoadOrderClicked" Content="Go to load order" />
                            </Grid>
                        </Border>
                        <Grid Grid.Row="2" Margin="0,0,0,8">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="210" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <CheckBox x:Name="ShowNonConflictingFilesCheckBox" VerticalAlignment="Center" Checked="ArchiveTreeFilterChanged" Unchecked="ArchiveTreeFilterChanged" Content="Show non-conflicting files" />
                            <StackPanel Grid.Column="1" Margin="8,0,0,0">
                                <TextBlock Margin="0,0,0,3" FontSize="10" Foreground="{StaticResource Muted}" Text="MOD OR ARCHIVE FILTER" />
                                <TextBox x:Name="ArchiveModFilterTextBox" TextChanged="ArchiveTreeFilterChanged" />
                            </StackPanel>
                            <StackPanel Grid.Column="2" Margin="8,0,0,0">
                                <TextBlock Margin="0,0,0,3" FontSize="10" Foreground="{StaticResource Muted}" Text="FILE OR COMPETING ARCHIVE FILTER" />
                                <TextBox x:Name="ArchiveFileFilterTextBox" TextChanged="ArchiveTreeFilterChanged" />
                            </StackPanel>
                            <Button x:Name="ClearArchiveFiltersButton" Grid.Column="3" Margin="8,13,0,0" Click="ClearArchiveFiltersClicked" Content="Clear filters" />
                        </Grid>
                        <Grid Grid.Row="3">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="12" />
                            </Grid.ColumnDefinitions>
                            <TreeView x:Name="ArchiveConflictTreeView" ScrollViewer.CanContentScroll="False" ScrollViewer.VerticalScrollBarVisibility="Hidden" SelectedItemChanged="ArchiveConflictTreeSelectionChanged" MouseDoubleClick="ArchiveConflictTreeDoubleClicked" />
                            <Grid Grid.Column="1">
                                <local:ArchiveOverviewBar x:Name="ConflictOverviewBar" Focusable="False" IsHitTestVisible="False" AutomationProperties.Name="Selected archive relationships in conflict results" />
                                <ScrollBar x:Name="ConflictScrollBar" Style="{StaticResource OverviewScrollBar}" AutomationProperties.Name="Conflict-results scrollbar" />
                            </Grid>
                        </Grid>
                        <Border Grid.Row="4" Margin="0,9,0,0" Padding="10" Background="#0F1618" BorderBrush="#2A393C" BorderThickness="1">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="330" />
                                </Grid.ColumnDefinitions>
                                <StackPanel>
                                    <TextBlock x:Name="ArchiveSelectedTitleTextBlock" FontWeight="SemiBold" Text="Select an archive or file" TextWrapping="Wrap" />
                                    <TextBlock x:Name="ArchiveSelectedMeaningTextBlock" Margin="0,4,0,0" Foreground="#B7C6C4" Text="Expand an archive to inspect Winning, Losing, Same content, and No conflicts inline." TextWrapping="Wrap" />
                                </StackPanel>
                                <Expander Grid.Column="1" Header="Details">
                                    <TextBlock x:Name="ArchiveSelectedTechnicalTextBlock" Margin="8,5,0,0" FontFamily="Consolas" FontSize="11" Foreground="#849694" TextWrapping="Wrap" />
                                </Expander>
                            </Grid>
                        </Border>
                    </Grid>
                </Grid>
            </TabItem>
            <TabItem Header="Code interactions">
                <Grid Margin="14">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="0" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="2*" MinHeight="110" />
                        <RowDefinition Height="6" />
                        <RowDefinition Height="3*" MinHeight="240" />
                    </Grid.RowDefinitions>
                    <Expander x:Name="CodeSummaryExpander" IsExpanded="True" Header="Code interactions · what to check">
                        <Grid Margin="0,6,0,0">
                            <Grid.RowDefinitions><RowDefinition Height="Auto" /><RowDefinition Height="Auto" /></Grid.RowDefinitions>
                            <Grid>
                                <Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
                                <TextBlock Foreground="{StaticResource Muted}" Text="Confirmed conflicts are exact boundaries where active changes cannot coexist. Review rows are not confirmed conflicts; each names one uncertainty to check. Identical overlaps remain available but are hidden from the starting view." TextWrapping="Wrap" />
                                <TextBlock x:Name="ColumnResizeHint" Grid.Column="1" Margin="18,0,0,0" Foreground="#80F8FF" FontWeight="SemiBold" Text="COLUMNS  ↔  Drag a divider to resize" />
                            </Grid>
                            <UniformGrid Grid.Row="1" Margin="0,10,0,0" Columns="4">
                                <Border Padding="10" Margin="0,0,8,0" Background="#151E20" BorderBrush="#2A393C" BorderThickness="1">
                                    <StackPanel><TextBlock Foreground="#FF4964" Text="CONFIRMED CONFLICTS" /><TextBlock x:Name="AttentionCountTextBlock" FontSize="24" Text="0" /></StackPanel>
                                </Border>
                                <Border Padding="10" Margin="0,0,8,0" Background="#151E20" BorderBrush="#2A393C" BorderThickness="1">
                                    <StackPanel><TextBlock Foreground="#FCEE0A" Text="NEEDS REVIEW" /><TextBlock x:Name="ReviewCountTextBlock" FontSize="24" Text="0" /></StackPanel>
                                </Border>
                                <Border Padding="10" Margin="0,0,8,0" Background="#151E20" BorderBrush="#2A393C" BorderThickness="1">
                                    <StackPanel><TextBlock Foreground="#B48CFF" Text="REVIEWED" /><TextBlock x:Name="ReviewedCountTextBlock" FontSize="24" Text="0" /></StackPanel>
                                </Border>
                                <Border Padding="10" Background="#151E20" BorderBrush="#2A393C" BorderThickness="1">
                                    <StackPanel><TextBlock Foreground="#42E58B" Text="IDENTICAL OVERLAPS" /><TextBlock x:Name="NoActionCountTextBlock" FontSize="24" Text="0" /></StackPanel>
                                </Border>
                            </UniformGrid>
                        </Grid>
                    </Expander>
                    <Grid Grid.Row="2" Margin="0,10,0,8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="180" />
                            <ColumnDefinition Width="200" />
                            <ColumnDefinition Width="250" />
                        </Grid.ColumnDefinitions>
                        <TextBox x:Name="QueueSearchTextBox" TextChanged="QueueFilterChanged" ToolTip="Search target, provider, or summary" AutomationProperties.Name="Search code conflicts" />
                        <ComboBox x:Name="QueueViewComboBox" Grid.Column="1" Margin="8,0,0,0" SelectionChanged="QueueFilterChanged">
                            <ComboBoxItem Tag="Actionable" IsSelected="True">Conflicts and review</ComboBoxItem>
                            <ComboBoxItem Tag="Proven">Confirmed conflicts</ComboBoxItem>
                            <ComboBoxItem Tag="NeedsDecision">Needs review</ComboBoxItem>
                            <ComboBoxItem Tag="Reviewed">Reviewed</ComboBoxItem>
                            <ComboBoxItem Tag="Compatible">Identical overlaps</ComboBoxItem>
                            <ComboBoxItem Tag="All">Everything found</ComboBoxItem>
                        </ComboBox>
                        <ComboBox x:Name="QueueSurfaceComboBox" Grid.Column="2" Margin="8,0,0,0" SelectionChanged="QueueFilterChanged">
                            <ComboBoxItem Tag="All" IsSelected="True">Everything</ComboBoxItem>
                            <ComboBoxItem Tag="VirtualFile">Code file overrides</ComboBoxItem>
                            <ComboBoxItem Tag="ScriptAndTweak">RedScript, CET, TweakXL</ComboBoxItem>
                            <ComboBoxItem Tag="SharedState">Shared values and targets</ComboBoxItem>
                            <ComboBoxItem Tag="ArchiveXl">ArchiveXL changes</ComboBoxItem>
                            <ComboBoxItem Tag="Diagnostic">Scan problems</ComboBoxItem>
                        </ComboBox>
                        <ComboBox x:Name="QueueProviderComboBox" Grid.Column="3" Margin="8,0,0,0" SelectionChanged="QueueFilterChanged" />
                    </Grid>
                    <DataGrid x:Name="WorkQueueDataGrid" Grid.Row="3" CanUserResizeColumns="True" SelectionMode="Extended" SelectionUnit="FullRow" ScrollViewer.HorizontalScrollBarVisibility="Auto" SelectionChanged="QueueSelectionChanged">
                        <DataGrid.CellStyle>
                            <Style TargetType="DataGridCell">
                                <Setter Property="Background" Value="Transparent" />
                                <Setter Property="Foreground" Value="#F2F6F7" />
                                <Setter Property="BorderBrush" Value="#2B4653" />
                                <Setter Property="BorderThickness" Value="0,0,1,0" />
                                <Style.Triggers>
                                    <Trigger Property="IsSelected" Value="True"><Setter Property="Background" Value="#075A66" /><Setter Property="Foreground" Value="#FFFFFF" /></Trigger>
                                </Style.Triggers>
                            </Style>
                        </DataGrid.CellStyle>
                        <DataGrid.RowStyle>
                            <Style TargetType="DataGridRow">
                                <Setter Property="BorderThickness" Value="4,0,0,0" />
                                <Setter Property="BorderBrush" Value="Transparent" />
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding CaseKind}" Value="ProvenConflict"><Setter Property="Background" Value="#310913" /><Setter Property="BorderBrush" Value="#FF304F" /></DataTrigger>
                                    <DataTrigger Binding="{Binding CaseKind}" Value="CompetingDeclaration"><Setter Property="Background" Value="#301A0A" /><Setter Property="BorderBrush" Value="#FF9A45" /></DataTrigger>
                                    <DataTrigger Binding="{Binding CaseKind}" Value="FileOverride"><Setter Property="Background" Value="#06242A" /><Setter Property="BorderBrush" Value="#00F0FF" /></DataTrigger>
                                    <DataTrigger Binding="{Binding CaseKind}" Value="OrderSensitive"><Setter Property="Background" Value="#321701" /><Setter Property="BorderBrush" Value="#FF7A00" /></DataTrigger>
                                    <DataTrigger Binding="{Binding CaseKind}" Value="CompilerEvidence"><Setter Property="Background" Value="#241334" /><Setter Property="BorderBrush" Value="#B48CFF" /></DataTrigger>
                                    <DataTrigger Binding="{Binding CaseKind}" Value="RuntimeCheck"><Setter Property="Background" Value="#292600" /><Setter Property="BorderBrush" Value="#FCEE0A" /></DataTrigger>
                                    <DataTrigger Binding="{Binding CaseKind}" Value="Composes"><Setter Property="Background" Value="#08251B" /><Setter Property="BorderBrush" Value="#42E58B" /></DataTrigger>
                                    <DataTrigger Binding="{Binding CaseKind}" Value="SharedTarget"><Setter Property="Background" Value="#111A22" /><Setter Property="BorderBrush" Value="#728A96" /></DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </DataGrid.RowStyle>
                        <DataGrid.Columns>
                            <DataGridTemplateColumn Header="What CS found" Width="245">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock FontWeight="SemiBold" Text="{Binding ClassificationLabel}" TextTrimming="CharacterEllipsis" ToolTip="{Binding Summary}">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="Foreground" Value="#D7E0DE" />
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding CaseKind}" Value="ProvenConflict"><Setter Property="Foreground" Value="#FF6A7E" /></DataTrigger>
                                                        <DataTrigger Binding="{Binding CaseKind}" Value="CompetingDeclaration"><Setter Property="Foreground" Value="#FFC078" /></DataTrigger>
                                                        <DataTrigger Binding="{Binding CaseKind}" Value="FileOverride"><Setter Property="Foreground" Value="#80F8FF" /></DataTrigger>
                                                        <DataTrigger Binding="{Binding CaseKind}" Value="OrderSensitive"><Setter Property="Foreground" Value="#FFB15A" /></DataTrigger>
                                                        <DataTrigger Binding="{Binding CaseKind}" Value="CompilerEvidence"><Setter Property="Foreground" Value="#D4B8FF" /></DataTrigger>
                                                        <DataTrigger Binding="{Binding CaseKind}" Value="RuntimeCheck"><Setter Property="Foreground" Value="#FFF67A" /></DataTrigger>
                                                        <DataTrigger Binding="{Binding CaseKind}" Value="Composes"><Setter Property="Foreground" Value="#7EF0B2" /></DataTrigger>
                                                        <DataTrigger Binding="{Binding IsSelected, RelativeSource={RelativeSource AncestorType=DataGridCell}}" Value="True"><Setter Property="Foreground" Value="#FFFFFF" /></DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            <DataGridTextColumn Header="Why it is here" Binding="{Binding ProofLabel}" Width="320" />
                            <DataGridTextColumn Header="Area" Binding="{Binding SurfaceLabel}" Width="150" />
                            <DataGridTemplateColumn Header="Exact target" Width="520">
                                <DataGridTemplateColumn.CellTemplate><DataTemplate><TextBlock Text="{Binding Target}" TextTrimming="CharacterEllipsis" ToolTip="{Binding Target}" /></DataTemplate></DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            <DataGridTemplateColumn Header="Mods involved" Width="420">
                                <DataGridTemplateColumn.CellTemplate><DataTemplate><TextBlock Text="{Binding ProviderSummary}" TextTrimming="CharacterEllipsis" ToolTip="{Binding ProviderSummary}" /></DataTemplate></DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                    <GridSplitter x:Name="CodeDetailSplitter" Grid.Row="4" Height="6" HorizontalAlignment="Stretch" VerticalAlignment="Center" Background="#314346" ResizeDirection="Rows" ResizeBehavior="PreviousAndNext" Cursor="SizeNS" />
                    <ScrollViewer x:Name="CodeCaseDetailScrollViewer" Grid.Row="5" Margin="0,3,0,0" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                        <Border Padding="12" Background="#0F1618" BorderBrush="#2A393C" BorderThickness="1">
                            <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="350" />
                            </Grid.ColumnDefinitions>
                            <StackPanel>
                                <TextBlock x:Name="SelectedClassificationTextBlock" Foreground="#FFD166" Text="Select a case" />
                                <TextBlock x:Name="SelectedTargetTextBlock" Margin="0,3,0,0" FontSize="17" FontWeight="SemiBold" Text="Select a row to see details" TextWrapping="Wrap" />
                                <TextBlock Margin="0,9,0,0" FontSize="10" Foreground="{StaticResource Muted}" Text="WHAT THIS MEANS" />
                                <TextBlock x:Name="SelectedMeaningTextBlock" Margin="0,3,0,0" Foreground="#E7ECEB" TextWrapping="Wrap" />
                                <TextBlock Margin="0,8,0,0" FontSize="10" Foreground="{StaticResource Muted}" Text="WHAT THE SOURCE SHOWS" />
                                <TextBlock x:Name="SelectedSummaryTextBlock" Margin="0,3,0,0" Foreground="#BAC7C5" TextWrapping="Wrap" />
                                <TextBlock Margin="0,8,0,0" FontSize="10" Foreground="{StaticResource Muted}" Text="WHY CS SHOWED IT" />
                                <TextBlock x:Name="SelectedProofTextBlock" Margin="0,3,0,0" Foreground="#E7ECEB" TextWrapping="Wrap" />
                                <TextBlock Margin="0,8,0,0" FontSize="10" Foreground="{StaticResource Muted}" Text="WHAT THIS DOES NOT PROVE" />
                                <TextBlock x:Name="SelectedBoundaryTextBlock" Margin="0,3,0,0" Foreground="#9FB0AE" TextWrapping="Wrap" />
                                <TextBlock Margin="0,8,0,0" FontSize="10" Foreground="{StaticResource Muted}" Text="MODS INVOLVED" />
                                <TextBlock x:Name="SelectedProvidersTextBlock" Margin="0,3,0,0" Foreground="#BAC7C5" TextWrapping="Wrap" />
                                <TextBlock Margin="0,8,0,0" FontSize="10" Foreground="{StaticResource Muted}" Text="WHAT TO DO" />
                                <TextBlock x:Name="SelectedActionTextBlock" Margin="0,3,0,0" Foreground="{StaticResource Accent}" TextWrapping="Wrap" />
                                <TextBlock x:Name="SelectedHashTextBlock" Margin="0,8,0,0" FontFamily="Consolas" FontSize="10" Foreground="#657775" />
                                <Expander x:Name="TechnicalEvidenceExpander" Margin="0,8,0,0" Header="Technical details">
                                    <Grid Margin="0,5,0,0">
                                        <Grid.RowDefinitions><RowDefinition Height="Auto" /><RowDefinition Height="Auto" /></Grid.RowDefinitions>
                                        <Button x:Name="CopyEvidenceButton" HorizontalAlignment="Right" IsEnabled="False" Click="CopyEvidenceClicked" Content="Copy details" />
                                        <TextBox x:Name="ExpertEvidenceTextBox" Grid.Row="1" Margin="0,6,0,0" Height="170" FontFamily="Consolas" FontSize="11" IsReadOnly="True" AcceptsReturn="True" TextWrapping="NoWrap" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto" AutomationProperties.Name="Selected technical evidence" />
                                    </Grid>
                                </Expander>
                            </StackPanel>
                            <StackPanel Grid.Column="1" Margin="14,0,0,0">
                                <TextBlock Foreground="{StaticResource Muted}" Text="REVIEW OUTCOME" />
                                <ComboBox x:Name="ReviewOutcomeComboBox" Margin="0,5,0,0">
                                    <ComboBoxItem Tag="Works as intended">Works as intended</ComboBoxItem>
                                    <ComboBoxItem Tag="Compatibility patch installed">Compatibility patch installed</ComboBoxItem>
                                    <ComboBoxItem Tag="One mod intentionally owns this">One mod intentionally owns this</ComboBoxItem>
                                    <ComboBoxItem Tag="Ignore for this profile">Ignore for this profile</ComboBoxItem>
                                </ComboBox>
                                <TextBox x:Name="ReviewRationaleTextBox" Margin="0,5,0,0" Height="54" AcceptsReturn="True" TextWrapping="Wrap" ToolTip="Optional notes" />
                                <UniformGrid Margin="0,7,0,0" Columns="2">
                                    <Button x:Name="SaveReviewButton" Click="ReviewClicked" Content="Save review" />
                                    <Button x:Name="ReopenButton" Margin="7,0,0,0" Click="ReopenClicked" Content="Reopen" />
                                </UniformGrid>
                                <TextBlock Margin="0,10,0,0" Foreground="{StaticResource Muted}" Text="OPEN MOD" />
                                <ComboBox x:Name="SelectedProviderComboBox" Margin="0,5,0,0" />
                                <UniformGrid Margin="0,7,0,0" Columns="3">
                                    <Button Click="OpenCodeProviderClicked" Content="Open folder" />
                                    <Button Margin="7,0,0,0" Click="CopyActionClicked" Content="Copy next step" />
                                    <Button x:Name="ShowTechnicalEvidenceButton" Margin="7,0,0,0" Click="ShowTechnicalEvidenceClicked" Content="View evidence" />
                                </UniformGrid>
                            </StackPanel>
                            </Grid>
                        </Border>
                    </ScrollViewer>
                </Grid>
            </TabItem>
            <TabItem x:Name="SupportTab" Header="Support">
                <Grid Margin="14">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <StackPanel>
                            <TextBlock FontSize="22" FontWeight="SemiBold" Text="Support" />
                            <TextBlock x:Name="DiagnosticsSummaryTextBlock" Margin="0,3,0,0" Foreground="{StaticResource Muted}" Text="No scan has run. Reproduce a problem once, then copy the report below." />
                        </StackPanel>
                        <StackPanel Grid.Column="1" Orientation="Horizontal">
                            <Button x:Name="ExportButton" Margin="0,0,8,0" IsEnabled="False" Click="ExportClicked" Content="Export support bundle" ToolTip="Save the scan details, your reviews, and any requested checks for troubleshooting." />
                            <Button Margin="8,0,0,0" Click="CopyDiagnosticReportClicked" Content="Copy report" />
                            <Button Margin="8,0,0,0" Click="SaveDiagnosticReportClicked" Content="Save report" />
                            <Button Margin="8,0,0,0" Click="OpenDiagnosticsFolderClicked" Content="Open logs folder" />
                        </StackPanel>
                    </Grid>
                    <Border Grid.Row="1" Margin="0,10,0,0" Padding="10,7" Background="#171F21" BorderBrush="#2A393C" BorderThickness="1">
                        <TextBlock Text="The report includes the current scan, actions you took, and recorded errors. It does not include mod source files and removes local paths." Foreground="#B7C6C4" TextWrapping="Wrap" />
                    </Border>
                    <TextBox x:Name="DiagnosticsTextBox" Grid.Row="2" Margin="0,8,0,0" FontFamily="Consolas" FontSize="12" IsReadOnly="True" AcceptsReturn="True" TextWrapping="NoWrap" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto" AutomationProperties.Name="Current support report" />
                    <Expander Grid.Row="3" Margin="0,8,0,0" Header="Raw application error log">
                        <TextBox x:Name="HistoricalDiagnosticsTextBox" Margin="0,6,0,0" Height="160" FontFamily="Consolas" FontSize="11" IsReadOnly="True" AcceptsReturn="True" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto" AutomationProperties.Name="Raw application error log" />
                    </Expander>
                </Grid>
            </TabItem>
        </TabControl>
        <Border Grid.Row="3" Background="#0B1218" BorderBrush="#28404D" BorderThickness="0,1,0,0">
            <Grid Margin="14,0">
                <TextBlock x:Name="FooterStatusTextBlock" VerticalAlignment="Center" Foreground="{StaticResource Muted}" Text="Ready" />
                <StackPanel HorizontalAlignment="Right" Orientation="Horizontal">
                    <TextBlock x:Name="ProfileBadgeTextBlock" VerticalAlignment="Center" Foreground="{StaticResource Accent}" Text="NO PROFILE" />
                </StackPanel>
            </Grid>
        </Border>
        <TextBlock x:Name="ErrorTextBlock" Grid.Row="3" Margin="14,0,430,0" VerticalAlignment="Center" Foreground="#FF806A" Visibility="Collapsed" />
    </Grid>
</Window>
